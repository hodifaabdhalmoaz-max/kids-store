# نظام البحث المتقدم - متجر الأطفال

## نظرة عامة
تم تطوير نظام بحث متقدم وذكي للمتجر الإلكتروني للأطفال يوفر تجربة بحث سلسة وفعالة للمستخدمين.

## الميزات الجديدة

### 1. البحث الذكي والمتقدم
- **البحث النصي المحسن**: يدعم البحث في أسماء المنتجات، الوصف المختصر، الوصف الكامل، ورقم SKU
- **البحث في العلاقات**: يبحث في أسماء الفئات، العلامات التجارية، الألوان، والأحجام
- **البحث بالكلمات المتعددة**: يدعم البحث بعدة كلمات مفتاحية منفصلة
- **البحث الآمن**: حماية من SQL injection وتنظيف المدخلات

### 2. البحث السريع (Quick Search)
- **اقتراحات فورية**: عرض النتائج أثناء الكتابة
- **نتائج متنوعة**: عرض المنتجات، الفئات، والعلامات التجارية
- **واجهة تفاعلية**: تصميم جذاب مع صور وأسعار
- **استجابة سريعة**: تأخير 300ms لتحسين الأداء

### 3. فلاتر البحث المتقدمة
- **فلترة بالفئات**: اختيار فئة واحدة أو متعددة
- **فلترة بالعلامات التجارية**: اختيار علامة تجارية واحدة أو متعددة
- **فلترة بالألوان**: اختيار لون واحد أو متعدد مع عرض الألوان الفعلية
- **فلترة بالأحجام**: اختيار حجم واحد أو متعدد
- **فلترة بالأسعار**: نطاقات أسعار ديناميكية
- **فلترة بحالة المخزون**: متوفر، غير متوفر، إلخ
- **فلترة بالمنتجات المميزة**: عرض المنتجات المميزة فقط

### 4. خيارات الترتيب المحسنة
- **الأكثر صلة**: ترتيب ذكي حسب الصلة والأهمية
- **الأحدث/الأقدم**: ترتيب حسب تاريخ الإضافة
- **السعر**: من الأقل للأعلى أو العكس (مع مراعاة سعر التخفيض)
- **الاسم**: ترتيب أبجدي أو عكسي
- **المخزون**: ترتيب حسب الكمية المتوفرة

### 5. إحصائيات البحث
- **تسجيل عمليات البحث**: حفظ جميع عمليات البحث للتحليل
- **المصطلحات الشائعة**: عرض أكثر المصطلحات بحثاً
- **تحليل سلوك المستخدمين**: فهم احتياجات العملاء

## الملفات المحدثة

### 1. خدمة البحث (SearchService.php)
```php
app/Services/SearchService.php
```
- تم إعادة كتابة الخدمة بالكامل
- إضافة دوال جديدة للبحث المتقدم
- تحسين الأداء مع التخزين المؤقت
- دعم البحث بالعلاقات

### 2. كنترولر المتجر (ShopController.php)
```php
app/Http/Controllers/ShopController.php
```
- تحديث دالة البحث لدعم المعاملات الجديدة
- إضافة دالة البحث السريع
- تحسين التحقق من صحة البيانات

### 3. صفحة المتجر (shop.blade.php)
```php
resources/views/shop.blade.php
```
- تحديث واجهة الفلاتر
- إضافة فلاتر الألوان والأحجام التفاعلية
- تحسين JavaScript للتفاعل
- إضافة شريط معلومات البحث

### 4. التخطيط الرئيسي (app.blade.php)
```php
resources/views/layouts/app.blade.php
```
- تحديث شريط البحث الرئيسي
- إضافة JavaScript للبحث السريع
- تحسين واجهة البحث المنبثقة

### 5. ملف CSS المخصص
```css
public/assets/css/advanced-search.css
```
- تصميم محسن للبحث السريع
- تحسينات بصرية للفلاتر
- تأثيرات تفاعلية وانيميشن
- دعم الاستجابة للأجهزة المختلفة

### 6. المسارات (web.php)
```php
routes/web.php
```
- إضافة مسار البحث السريع
- تحديث مسارات البحث

## كيفية الاستخدام

### 1. البحث الأساسي
```
/shop/search?search=كلمة البحث
```

### 2. البحث مع الفلاتر
```
/shop/search?search=كلمة البحث&category=فئة&brand=علامة تجارية&min_price=100&max_price=500
```

### 3. البحث السريع (AJAX)
```javascript
GET /shop/quick-search?q=كلمة البحث
```

### 4. الترتيب
```
/shop/search?sort_by=price_low_high&sort_direction=asc
```

## المعاملات المدعومة

| المعامل | الوصف | مثال |
|---------|--------|-------|
| `search` | نص البحث | `search=لعبة` |
| `category` | الفئة (ID أو slug) | `category=العاب-اطفال` |
| `brand` | العلامة التجارية (ID أو slug) | `brand=disney` |
| `colors` | الألوان (IDs مفصولة بفاصلة) | `colors=1,2,3` |
| `sizes` | الأحجام (IDs مفصولة بفاصلة) | `sizes=1,2,3` |
| `min_price` | أقل سعر | `min_price=100` |
| `max_price` | أعلى سعر | `max_price=500` |
| `stock_status` | حالة المخزون | `stock_status=instock` |
| `featured` | المنتجات المميزة | `featured=1` |
| `sort_by` | نوع الترتيب | `sort_by=price_low_high` |
| `per_page` | عدد المنتجات في الصفحة | `per_page=24` |

## خيارات الترتيب

| القيمة | الوصف |
|--------|--------|
| `relevance` | الأكثر صلة (افتراضي) |
| `newest` | الأحدث |
| `oldest` | الأقدم |
| `price_low_high` | السعر: من الأقل للأعلى |
| `price_high_low` | السعر: من الأعلى للأقل |
| `name_a_z` | الاسم: أ - ي |
| `name_z_a` | الاسم: ي - أ |
| `stock` | الأكثر توفراً |

## الأمان والأداء

### الأمان
- تنظيف جميع المدخلات لمنع SQL injection
- التحقق من صحة البيانات
- حماية من XSS attacks

### الأداء
- استخدام التخزين المؤقت للفلاتر
- فهرسة قاعدة البيانات المحسنة
- تحسين استعلامات SQL
- تأخير البحث السريع لتقليل الطلبات

## التخصيص

### إضافة فلاتر جديدة
1. تحديث `SearchService.php`
2. إضافة الحقول في `shop.blade.php`
3. تحديث JavaScript للتفاعل

### تخصيص التصميم
1. تعديل `advanced-search.css`
2. تحديث قوالب Blade
3. تخصيص JavaScript

## الاختبار

### اختبار البحث الأساسي
```bash
curl "http://localhost:8000/shop/search?search=لعبة"
```

### اختبار البحث السريع
```bash
curl "http://localhost:8000/shop/quick-search?q=لعبة"
```

### اختبار الفلاتر
```bash
curl "http://localhost:8000/shop/search?category=1&brand=1&min_price=100&max_price=500"
```

## الصيانة

### تنظيف إحصائيات البحث
```php
// حذف الإحصائيات الأقدم من 6 أشهر
DB::table('statistics')
  ->where('type', 'search')
  ->where('created_at', '<', now()->subMonths(6))
  ->delete();
```

### تحديث التخزين المؤقت
```php
Cache::forget('search_filters');
Cache::forget('popular_search_terms');
```

## المتطلبات التقنية

- Laravel 11+
- PHP 8.1+
- MySQL 8.0+
- jQuery 3.6+
- Bootstrap 5+

## الدعم والتطوير

تم تطوير هذا النظام بواسطة Hudhaifa Al-Hudhaifi مع التركيز على:
- تجربة المستخدم المحسنة
- الأداء العالي
- الأمان المتقدم
- سهولة الصيانة والتطوير

---

**ملاحظة**: جميع النصوص والواجهات باللغة العربية لتناسب الجمهور المستهدف من المتحدثين بالعربية.
